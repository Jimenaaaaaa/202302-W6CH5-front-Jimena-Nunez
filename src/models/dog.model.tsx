export type ProtoDogStructure = {
  name: string;
  breed: string;
  weight: number;
  is_good: boolean;
};

export type DogStructure = {
  id: number;
  name: string;
  breed: string;
  weight: number;
  is_good: boolean;
};
